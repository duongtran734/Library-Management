@model LibraryManagment.Models.ViewModels.CheckoutModels

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>
<div class="container">
    <h2>Checkout Library Item</h2>
    <div class="jumbotron">
        <div class="col-md-3">
            <div>
                <p>@Model.Title</p>
                <img src=@Model.ImgUlr alt="img" />
            </div>
        </div>
        <div class="col-md-9">
            @using(Html.BeginForm("PlaceCheckout", "Checkouts", FormMethod.Post))
            {
                @Html.HiddenFor(a=>a.AssetId)
                <div>
                    Please insert Library Card Id:
                    @Html.TextBoxFor(a=>a.LibraryCardId)
                </div>
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>
            }
        </div>
    </div>
</div>


<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
